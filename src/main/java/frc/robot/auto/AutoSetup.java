package frc.robot.auto;

import com.ctre.phoenix6.swerve.SwerveModuleConstants;
import com.pathplanner.lib.config.ModuleConfig;
import com.pathplanner.lib.config.RobotConfig;
import com.pathplanner.lib.path.PathConstraints;
import frc.robot.util.constants.RobotPhysicalConstants;
import frc.robot.subsystems.swerve.SwerveConstants;
import frc.robot.util.constants.DrivingConstants;

public class AutoSetup {
  /*
   * public AutoSetup() {
   * // Pythagorean theorem for diagonal length
   * CSPPathing.setRobotDimensions(Math.sqrt(
   * Math.pow(RobotPhysicalConstants.ROBOT_LENGTH, 2) +
   * Math.pow(RobotPhysicalConstants.ROBOT_WIDTH, 2)));
   * 
   * CSPPathing.addObstacle(cornerPose, nonAdjacentCornerPose);
   * 
   * CSPPathing.configureConstraints(pathConstraints, robotConfig);
   * }
   * 
   * // template pathconstraints
   * public PathConstraints pathConstraints = new PathConstraints(
   * DrivingConstants.DRIVE_MAX_VEL, DrivingConstants.DRIVE_MAX_ACC,
   * DrivingConstants.TURN_MAX_VEL, DrivingConstants.TURN_MAX_ACC);
   * 
   * public RobotConfig robotConfig = new RobotConfig(MASS, MOI,
   * new ModuleConfig(, MAX_VEL, COEFFICIENT_OF_FRICTION,
   * driveMotor, CURRENT_LIMIT, NUM_OF_MOTORS),
   * moduleOffsets);
   * 
   * CSPPathing.configureNodes(CSPPathing.NavNode(idString1,nodePose1,List.of(
   * idString2,idString3)),CSPPathing.NavNode(idString2,nodePose2,List.of(
   * idString1,idString3)),CSPPathing.NavNode(idString3,nodePose3,List.of(
   * idString1,idString2)),...);
   */
}
